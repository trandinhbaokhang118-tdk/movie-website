import { AdminPageHead } from "../AdminPageHead";
import { listAdminAccounts } from "@/db/runtime";
import { setAccountRoleAction } from "@/app/actions/admin";
import { requireAdminCapability } from "../access";
const roleLabels={viewer:"Người xem",admin:"Super Admin",super_admin:"Super Admin",content_manager:"Content Manager",support:"Support",analyst:"Analyst"} as const;
export default async function Page(){const {viewer}=await requireAdminCapability("permissions");const accounts=await listAdminAccounts();return <div className="admin-dashboard"><AdminPageHead eyebrow="ACCESS CONTROL" title="Vai trò & phân quyền" description="Phân quyền server-side theo nguyên tắc tối thiểu; thay đổi được ghi vào audit log."/><section className="admin-panel admin-simple-list"><p><b>Super Admin</b> · Toàn quyền và gán vai trò</p><p><b>Content Manager</b> · Catalog, lịch, blog và podcast</p><p><b>Support</b> · Người dùng và nhật ký hỗ trợ</p><p><b>Analyst</b> · Tổng quan và phân tích chỉ đọc</p></section><div className="admin-table-v2"><div className="admin-table-head"><span>Người dùng</span><span>Hiện tại</span><span>Vai trò mới</span><span>Thao tác</span></div>{accounts.map(account=><form className="admin-table-row" action={setAccountRoleAction} key={account.id}><span><i>{account.displayName.slice(0,2).toUpperCase()}</i><span><b>{account.displayName}</b><small>{account.email}</small></span></span><span>{roleLabels[account.role as keyof typeof roleLabels]??account.role}</span><span><input type="hidden" name="userId" value={account.id}/><select name="role" defaultValue={account.role==="admin"?"super_admin":account.role} disabled={account.id===viewer.id}><option value="viewer">Người xem</option><option value="analyst">Analyst</option><option value="support">Support</option><option value="content_manager">Content Manager</option><option value="super_admin">Super Admin</option></select></span><span>{account.id===viewer.id?<small>Không tự đổi vai trò</small>:<button type="submit">Cập nhật</button>}</span></form>)}</div></div>}
